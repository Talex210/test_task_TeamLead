{
  "name": "jira-project-assistant",
  "version": "1.0.0",
  "description": "Jira Project Assistant - помощник для управления проектом в Jira",
  "main": "index.js",
  "scripts": {
    "dev": "forge tunnel",
    "build": "cd static/ui && npm run build",
    "deploy": "npm run build && forge deploy --no-verify",
    "deploy-verbose": "npm run build && forge deploy --no-verify --verbose",
    "install": "forge install",
    "setup": "cd static/ui && npm install",
    "clean-build": "cd static/ui && rm -rf build && npm run build",
    "clean-all": "cd static/ui && rm -rf build node_modules && npm install && npm run build",
    "check-build": "cd static/ui && npm run build && ls -la build/",
    "quick-deploy": "forge deploy --no-verify"
  },
  "dependencies": {
    "@forge/api": "^3.0.0",
    "@forge/resolver": "^1.5.0"
  },
  "devDependencies": {
    "@forge/cli": "^12.5.2"
  },
  "author": "Alex_Tol",
  "license": "MIT"
}